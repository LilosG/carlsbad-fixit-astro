{
  "buildCommand": "npm run build",
  "outputDirectory": "dist",
  "ignoreCommand": "bash -lc 'if [ \"$VERCEL_GIT_COMMIT_REF\" = \"main\" ]; then exit 1; else echo Skipping non-main branch \"$VERCEL_GIT_COMMIT_REF\"; exit 0; fi'"
}
